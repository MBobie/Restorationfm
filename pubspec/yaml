name: restoration_fm_radio_app
description: "Restoration FM Radio App"
publish_to: 'none' # Set this if the app is not intended for publishing to pub.dev

version: 1.0.0+1 # Semantic versioning: MAJOR.MINOR.PATCH+BUILD

environment:
  sdk: ">=3.5.0 <4.0.0" # Ensure your Flutter and Dart SDK versions match this range

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.11 # Handles launching external URLs
  cupertino_icons: ^1.0.8 # Provides iOS-style icons
  badges: ^3.0.0 # Badge widget library (remove if unused)
  audioplayers: ^0.20.1
  flutter_local_notifications: ^15.0.0
  image_picker: ^0.8.8+7
  permission_handler: ^10.6.0
  flutter_sound: ^9.2.15



dev_dependencies:
  flutter_test:
    sdk: flutter # For testing your Flutter app
  flutter_lints: ^2.0.1 # Linting rules for Dart/Flutter

flutter:
  uses-material-design: true # Enables Material Design in your app
  assets:
    - assets/images/placeholder.png # Ensure this path exists in your project
